---
import BaseLayout from "../layouts/BaseLayout.astro";
import AnimatedCounter from "../components/ui/AnimatedCounter.astro";
import VectorMapWrapper from "../components/features/map/VectorMapWrapper.astro";
import "../styles/user.css";

// === DATOS CORPORATIVOS ===
const BRANDS = [
/*   { name: "Telcel", logo: "https://upload.wikimedia.org/wikipedia/commons/8/8e/Telcel_logo.svg" }, */
  { name: "Apple", logo: "https://cdn.worldvectorlogo.com/logos/apple-13.svg" },
  { name: "Samsung", logo: "https://cdn.worldvectorlogo.com/logos/samsung-8.svg" },
  { name: "Xiaomi", logo: "https://cdn.worldvectorlogo.com/logos/xiaomi-3.svg" },
  { name: "Motorola", logo: "https://cdn.worldvectorlogo.com/logos/motorola-new-logo.svg" },
  { name: "Huawei", logo: "https://cdn.worldvectorlogo.com/logos/huawei.svg" },
  { name: "Honor", logo: "https://cdn.worldvectorlogo.com/logos/huawei-honor-logo-1.svg" },
  { name: "Oppo", logo: "https://cdn.worldvectorlogo.com/logos/oppo-2022-1.svg" },
];

const VALUES = [
             { 
                title: "Fe en Dios", 
                desc: "Promover nuestra capacidad para alcanzar cada uno de los retos que se nos presentan a través de la confianza en nuestro Creador, quien es el que sustenta nuestra filosofía institucional.", 
                icon: "M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z",
                color: "text-amber-400",
                barColor: "bg-amber-400",
                bg: "group-hover:bg-amber-500/10",
                border: "group-hover:border-amber-500/50"
             },
             { 
                title: "Unidad", 
                desc: "Fomentar el trabajo en equipo, el compañerismo y la buena comunicación dentro de nuestra organización.", 
                icon: "M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0z",
                color: "text-blue-400",
                barColor: "bg-blue-400",
                bg: "group-hover:bg-blue-500/10",
                border: "group-hover:border-blue-500/50"
             },
             { 
                title: "Respeto", 
                desc: "Tratar a toda persona, sin importar nivel social, ideología o grupo étnico, de manera digna con tolerancia y cortesía.", 
                icon: "M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z",
                color: "text-cyan-400",
                barColor: "bg-cyan-400",
                bg: "group-hover:bg-cyan-500/10",
                border: "group-hover:border-cyan-500/50"
             },
             { 
                title: "Integridad y ética", 
                desc: "Conducirse intachablemente demostrando ser digno de confianza y mostrarse de una sola pieza. Actuando siempre con principios sólidos los cuales motivarán el efecto de nuestra productividad.", 
                icon: "M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z",
                color: "text-emerald-400",
                barColor: "bg-emerald-400",
                bg: "group-hover:bg-emerald-500/10",
                border: "group-hover:border-emerald-500/50"
             }
           ];
---

<BaseLayout
  title="Nuestra Esencia | Grupo Línea Digital"
  description="Más de 25 años conectando a México con valores sólidos, tecnología de punta y el respaldo de la red Telcel."
>
  <!-- === HERO SECTION: MAPA & COBERTURA === -->
  <section class="relative pt-12 md:pt-20 md:pb-24 lg:h-[calc(100vh-5rem)] mb-24 b-24 overflow-hidden">
    <div class="container relative z-10 mx-auto px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        
        <!-- COLUMNA TEXTO -->
        <div class="lg:order-first text-center lg:text-left animate-fade-in-up">
          <!-- Badge -->
          <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-blue-50 border border-blue-200/60 dark:bg-blue-900/20 dark:border-blue-500/20 mb-8 backdrop-blur-sm">
            <span class="w-2 h-2 rounded-full bg-blue-600 dark:bg-blue-400 animate-pulse"></span>
            <span class="text-xs font-bold uppercase tracking-widest text-blue-700 dark:text-blue-300">Distribuidor Autorizado Telcel</span>
          </div>
 <h1
        class="mx-auto max-w-4xl text-5xl font-bold tracking-tight text-slate-900 sm:text-7xl dark:text-white mb-6 text-balance"
      >
        Lideres en
        <span
          class="text-transparent bg-clip-text bg-gradient-to-b from-blue-400 to-blue-700 dark:from-blue-400 dark:to-blue-200"
        >
          Conectividad Regional
        </span>
      </h1>

        <p class="text-lg md:text-xl leading-relaxed text-slate-600 dark:text-slate-300 mb-10 max-w-xl mx-auto lg:mx-0 text-pretty font-medium">
  Más de <span class="font-bold text-slate-900 dark:text-white">25 años definiendo el estándar</span> de conectividad. 
  Somos tu enlace directo a la infraestructura móvil más avanzada de México, garantizando 
  soporte local y <span class="font-semibold text-blue-600 dark:text-blue-400">cobertura estratégica</span> 
  donde otros no llegan.
</p>

          <!-- Botones/Badges estáticos -->
          <div class="flex flex-wrap gap-4 justify-center lg:justify-start">
            <div class="flex items-center gap-3 px-5 py-3 rounded-2xl bg-white border border-slate-200 shadow-sm dark:bg-[#151e2e] dark:border-white/10">
              <span class="text-2xl">🇲🇽</span>
              <div class="flex flex-col text-left">
                  <span class="text-[10px] font-bold uppercase text-slate-400">Origen</span>
                  <span class="text-sm font-bold text-slate-900 dark:text-white">100% Mexicanos</span>
              </div>
            </div>
            <div class="flex items-center gap-3 px-5 py-3 rounded-2xl bg-white border border-slate-200 shadow-sm dark:bg-[#151e2e] dark:border-white/10">
              <span class="text-2xl">📡</span>
              <div class="flex flex-col text-left">
                  <span class="text-[10px] font-bold uppercase text-slate-400">Red</span>
                  <span class="text-sm font-bold text-slate-900 dark:text-white">5G Ready</span>
              </div>
            </div>
          </div>
        </div>

        <!-- COLUMNA VISUAL (MAPA INTERACTIVO) -->
        <div class="relative group perspective-1000">
          <!-- Glass Container -->
          <div class="relative rounded-[2.5rem] bg-white/80 dark:bg-[#151e2e]/80 border border-white/60 dark:border-white/10 shadow-2xl shadow-blue-900/10 dark:shadow-black/40 p-3 transition-shadow duration-700">
            <div class="rounded-[2rem] overflow-hidden bg-slate-50 dark:bg-[#0B1120] relative aspect-[1.47] w-full">
               
               <VectorMapWrapper />
            </div>
          </div>
        </div>

      </div>
    </div>
    <!-- === STATS DASHBOARD (High Authority) === -->
  <section class="relative pt-32 md:pt-32 -mt-12 md:-mt-20 z-20 md:pb-20 pb-8">
    <div class="container mx-auto max-w-6xl">
      
      <!-- Contenedor Glass -->
      <div class="relative overflow-hidden rounded-[2.5rem] bg-white dark:bg-[#1a2333]/90 backdrop-blur-xl border border-slate-200 dark:border-white/10 shadow-2xl shadow-blue-900/20 dark:shadow-black/50">
        
        <!-- Efectos de Luz Ambiental -->
        <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-blue-500 to-transparent opacity-50"></div>
        <div class="absolute -left-20 -top-20 w-64 h-64 bg-blue-500/10 rounded-full blur-3xl pointer-events-none"></div>

        <div class="grid grid-cols-2 md:grid-cols-4 items-center justify-center divide-x divide-slate-100 dark:divide-white/5 py-10 md:py-8">
          
          <!-- STAT 1: TRAYECTORIA (Trust Anchor) -->
          <div class="flex flex-col items-center text-center px-2 group cursor-default">
            <div class="mb-2 flex items-baseline">
                <span class="text-2xl md:text-3xl font-bold text-blue-600 dark:text-blue-500 mr-1 transform group-hover:-translate-y-1 transition-transform duration-300">+</span>
                <span class="text-4xl md:text-6xl font-black tracking-tighter text-slate-900 dark:text-white">
                    <AnimatedCounter end={25} duration={1500} />
                </span>
            </div>
            <p class="text-[10px] md:text-xs font-bold uppercase tracking-[0.15em] text-slate-500 dark:text-slate-400">
              Años de Trayectoria
            </p>
          </div>

          <!-- STAT 2: VOLUMEN (Social Proof) -->
          <div class="flex flex-col items-center text-center px-4 group cursor-default">
            <div class="mb-2 flex items-baseline">
                <span class="text-4xl md:text-6xl font-black tracking-tighter text-slate-900 dark:text-white">
                    <AnimatedCounter end={10} suffix="k" duration={2000} />
                </span>
            </div>
            <p class="text-[10px] md:text-xs font-bold uppercase tracking-[0.15em] text-slate-500 dark:text-slate-400 group-hover:text-blue-500 transition-colors">
              Usuarios Activos
            </p>
          </div>

          <!-- STAT 3: STOCK (Operational Power) -->
          <div class="flex flex-col items-center text-center px-4 group cursor-default">
            <div class="mb-2 flex items-baseline">
                <span class="text-2xl md:text-3xl font-bold text-slate-400 mr-1">+</span>
                <span class="text-4xl md:text-6xl font-black tracking-tighter text-slate-900 dark:text-white">
                    <AnimatedCounter end={1000} duration={2000} />
                </span>
            </div>
            <p class="text-[10px] md:text-xs font-bold uppercase tracking-[0.15em] text-slate-500 dark:text-slate-400">
              Equipos en Stock
            </p>
          </div>

          <!-- STAT 4: LIDERAZGO (Key Differentiator) -->
          <!-- Fondo sutilmente diferente para destacar el #1 -->
          <div class="flex flex-col items-center text-center px-4 py-2 relative overflow-hidden group cursor-default">
             <!-- Gradient Glow detrás del #1 -->
             <div class="absolute inset-0 bg-gradient-to-b from-green-500/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
             
            <div class="mb-2 relative z-10">
                <span class="text-4xl md:text-6xl font-black tracking-tighter text-transparent bg-clip-text bg-gradient-to-b from-green-400 to-green-600 drop-shadow-sm filter">
                    #1
                </span>
            </div>
            <div class="relative z-10 flex items-center gap-1.5">
                <span class="relative flex h-1.5 w-1.5">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-green-500"></span>
                </span>
                <p class="text-[10px] md:text-xs font-bold uppercase tracking-[0.15em] text-slate-600 dark:text-slate-300">
                  En el Sureste
                </p>
            </div>
          </div>

        </div>
      </div>
      
    </div>
  </section>
  </section>

  <!-- === BRAND MARQUEE (Efecto Infinito - Retail Power) === -->
  <section class="md:py-20 py-8 overflow-visible relative bg-slate-50 dark:bg-[#0f1520]">
      <!-- Gradiente de desvanecimiento superior - del color de fondo a transparente -->
      <div class="absolute top-0 left-0 right-0 h-32 md:h-40 bg-gradient-to-b from-transparent via-slate-50/30 via-slate-50/60 to-slate-50 dark:from-transparent dark:via-[#0f1520]/30 dark:via-[#0f1520]/60 dark:to-[#0f1520] pointer-events-none -translate-y-full"></div>
      
      <!-- Gradiente de desvanecimiento inferior - del color de fondo a transparente -->
      <div class="absolute bottom-0 left-0 right-0 h-32 md:h-40 bg-gradient-to-t from-transparent via-slate-50/30 via-slate-50/60 to-slate-50 dark:from-transparent dark:via-[#0f1520]/30 dark:via-[#0f1520]/60 dark:to-[#0f1520] pointer-events-none translate-y-full"></div>

      <div class="container mx-auto px-6 text-center mb-12 relative z-20">
 <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-blue-50 border border-blue-200/60 dark:bg-blue-900/20 dark:border-blue-500/20 mb-8 backdrop-blur-sm">
            <span class="w-2 h-2 rounded-full bg-blue-600 dark:bg-blue-400 animate-pulse"></span>
            <span class="text-xs font-bold uppercase tracking-widest text-blue-700 dark:text-blue-300">Ecosistema Tecnológico</span>
          </div>
                  <h2 class="text-3xl md:text-5xl font-black dark:text-white tracking-tight leading-tight">
            El mejor hardware <br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-700 via-blue-400 to-blue-700 dark:from-white dark:via-blue-300 dark:to-white">del mundo en tus manos.</span>
          </h2>
      </div>

      <!-- Marquee Wrapper -->
      <div class="relative w-full flex overflow-hidden mask-gradient">
          <div class="flex animate-marquee whitespace-nowrap py-4">
              {[...BRANDS, ...BRANDS, ...BRANDS].map((brand, index) => (
                  <div class="mx-8 md:mx-12 flex items-center justify-center grayscale hover:grayscale-0 opacity-50 hover:opacity-100 transition-all duration-500 cursor-default">
                      <img 
                        src={brand.logo} 
                        alt={brand.name} 
                        class="h-12 w-auto max-w-[120px] object-contain dark:invert dark:brightness-200" 
                        loading="lazy"
                      />
                  </div>
              ))}
          </div>
      </div>
  </section>

<!-- === FILOSOFÍA & VALORES (Deep Core Design) === -->
  <section class="relative mt-40 overflow-hidden">
    
    <!-- Ambient Background -->
    <div class="absolute inset-0 pointer-events-none">
        <div class="absolute top-1/2 right-0 -translate-y-1/2 w-[600px] h-[600px] bg-blue-900/20 rounded-full blur-[120px] opacity-60 mix-blend-screen"></div>
    </div>

    <div class="container mx-auto px-6 relative z-10">
      
      <!-- 1. HEADER NARRATIVO -->
      <div class="text-center max-w-3xl mx-auto mb-24">
        <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-blue-50 border border-blue-200/60 dark:bg-blue-900/20 dark:border-blue-500/20 mb-8 backdrop-blur-sm">
            <span class="w-2 h-2 rounded-full bg-blue-600 dark:bg-blue-400 animate-pulse"></span>
            <span class="text-xs font-bold uppercase tracking-widest text-blue-700 dark:text-blue-300">Nuestro Propósito</span>
          </div>
        
        <h2 class="text-4xl md:text-6xl font-black  mb-6 tracking-tight leading-tight">
          Tecnología con <br class="hidden md:block"/>
          <span class="text-transparent bg-clip-text bg-gradient-to-b from-blue-400 to-blue-700 dark:from-blue-400 dark:to-blue-200 dark:to-blue-200">Sentido Humano.</span>
        </h2>
        
        <p class="text-lg md:text-xl text-slate-600 dark:text-slate-300 leading-relaxed text-balance font-medium">
           Reinventamos la forma de dar productos financieros, logramos que sea fácil y rápido, creemos en la <span class="font-bold text-slate-900 dark:text-white">inclusión financiera</span> y que la tecnología llegue a todos. 
        </p>
      </div>

      <!-- 2. SISTEMA ORBITAL Y GRID -->
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 items-center">
        
        <!-- VISUALIZACIÓN ORBITAL (Columna Izquierda - Sticky en Desktop) -->
        <div class="lg:col-span-5 relative h-[500px] flex items-center justify-center lg:sticky lg:top-32 order-2 lg:order-1">
          <!-- A. EL NÚCLEO (The Brand Core) -->
          <div class="relative z-20 flex items-center justify-center">
              <!-- Glow pulsante -->
              <div class="absolute inset-0 bg-blue-600 rounded-full blur-3xl opacity-20 animate-pulse-slow"></div>
              <!-- Esfera central -->
              <div class="w-32 h-32 rounded-full bg-gradient-to-br from-blue-600 to-indigo-700 flex items-center justify-center shadow-[0_0_50px_rgba(37,99,235,0.5)] border border-white/20 relative z-10 overflow-hidden group">
                 <div class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-30 mix-blend-overlay"></div>
                 <!-- Icono central (Logo Abstracto) -->
                 <svg class="w-14 h-14 text-white drop-shadow-lg transform group-hover:scale-110 transition-transform duration-700" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
              </div>
          </div>

          <!-- B. ÓRBITAS (SVG Animado) -->
          <div class="absolute inset-0 pointer-events-none">
             <svg class="w-full h-full animate-[spin_60s_linear_infinite] opacity-60 dark:opacity-40" viewBox="0 0 500 500">
                <!-- Órbita Externa -->
                <circle cx="250" cy="250" r="240" fill="none" stroke="url(#orbitGrad)" stroke-width="1" stroke-dasharray="10" />
                <!-- Planeta Externo -->
                <circle cx="490" cy="250" r="8" fill="#3b82f6" filter="url(#glowFilter)" />
             </svg>
             
             <svg class="absolute inset-0 w-full h-full animate-[spin_40s_linear_infinite_reverse] opacity-80 dark:opacity-60" viewBox="0 0 500 500">
                <!-- Órbita Media -->
                <circle cx="250" cy="250" r="160" fill="none" class="stroke-slate-400/40 dark:stroke-white/10" stroke-width="1" />
                <!-- Planeta Medio -->
                <circle cx="250" cy="90" r="6" fill="#22d3ee" filter="url(#glowFilter)" />
             </svg>
             
             <svg class="absolute inset-0 w-full h-full animate-[spin_20s_linear_infinite]" viewBox="0 0 500 500">
                 <!-- Órbita Interna -->
                 <circle cx="250" cy="250" r="100" fill="none" class="stroke-slate-400/60 dark:stroke-white/20" stroke-width="1" stroke-dasharray="4 8" />
             </svg>
             
             <!-- Defs para SVGs -->
             <svg width="0" height="0">
                <defs>
                    <linearGradient id="orbitGrad" x1="0%" y1="0%" x2="100%" y2="0%">
                        <stop offset="0%" stop-color="rgba(59, 130, 246, 0)" />
                        <stop offset="50%" stop-color="rgba(59, 130, 246, 1)" />
                        <stop offset="100%" stop-color="rgba(59, 130, 246, 0)" />
                    </linearGradient>
                    <filter id="glowFilter" x="-50%" y="-50%" width="200%" height="200%">
                        <feGaussianBlur stdDeviation="4" result="coloredBlur"/>
                        <feMerge><feMergeNode in="coloredBlur"/><feMergeNode in="SourceGraphic"/></feMerge>
                    </filter>
                </defs>
             </svg>
          </div>
        </div>

        <!-- VALORES (Columna Derecha) -->
        <div class="lg:col-span-7 grid grid-cols-1 md:grid-cols-2 gap-6 order-1 lg:order-2">
           {VALUES.map(val => (
             <article class={`group relative overflow-hidden rounded-[2rem] dark:bg-[#101623] bg-white p-8 border border-white/5 transition-all duration-500 hover:-translate-y-1 ${val.border} hover:shadow-2xl dark:border-white/5`}>
                
                <!-- Background Reveal -->
                <div class={`absolute inset-0 opacity-0 transition-opacity duration-500 ${val.bg}`}></div>
                
                <div class="relative z-10">
                    <!-- Icono -->
                    <div class={`w-12 h-12 mb-6 rounded-xl dark:bg-white/5 bg-slate-600/10 flex items-center justify-center border dark:border-white/10 transition-transform duration-500 group-hover:scale-110 dark:group-hover:bg-white/10 group-hover:bg-slate-600/10 ${val.color}`}>
                        <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={val.icon} /></svg>
                    </div>

                    <!-- Texto -->
                    <h3 class="text-xl font-bold font-bold text-slate-900 dark:text-white mb-3 group-hover:text-slate-900dark:group-hover:text-white transition-colors">{val.title}</h3>
                    <p class="text-sm text-slate-400 leading-relaxed group-hover:text-slate-900 dark:group-hover:text-white transition-colors font-medium">
                        {val.desc}
                    </p>
                </div>

                <!-- Borde Inferior (Barra de progreso visual) - MOVIDO FUERA DEL PADDING -->
                <div class={`absolute bottom-0 left-0 h-[4px] w-0 group-hover:w-full transition-all duration-700 ease-out ${val.barColor}`}></div>
             </article>
           ))}
        </div>

      </div>
    </div>
  </section>

</BaseLayout>

<script>
  // Inicialización de partículas ambientales
  function initParticles() {
    const container = document.getElementById("particles-container");
    if (!container) return;
    container.innerHTML = '';

    const count = window.innerWidth < 768 ? 10 : 25;

    for (let i = 0; i < count; i++) {
      const p = document.createElement("div");
      
      // Alternar entre las dos animaciones
      const useAltAnimation = i % 2 === 0;
      const animationName = useAltAnimation ? 'float-alt' : 'float';
      
      p.className = `absolute rounded-full bg-blue-500 dark:bg-blue-400 blur-sm`;
      
      const size = Math.random() * 15 + 6; 
      const duration = Math.random() * 15 + 10;
      const delay = Math.random() * -20;
      
      p.style.cssText = `
        width: ${size}px;
        height: ${size}px;
        left: ${Math.random() * 100}%;
        top: ${Math.random() * 100}%;
        animation: ${animationName} ${duration}s ease-in-out infinite;
        animation-delay: ${delay}s;
        will-change: transform, opacity;
      `;
      
      container.appendChild(p);
    }
  }

  document.addEventListener("astro:page-load", initParticles);
  document.addEventListener("DOMContentLoaded", initParticles);
</script>

<style>
  /* Animación Marquee Infinito */
  @keyframes marquee {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }
  .animate-marquee {
    animation: marquee 40s linear infinite;
  }
  .animate-marquee:hover {
    animation-play-state: paused;
  }

  /* Máscara de desvanecimiento para el marquee - todos los lados */
  .mask-gradient {
    -webkit-mask-image: 
      linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 15%, black 85%, transparent 100%);
    -webkit-mask-size: 100% 100%;
    -webkit-mask-composite: source-in;
    mask-image: 
      linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 15%, black 85%, transparent 100%);
    mask-size: 100% 100%;
    mask-composite: intersect;
  }

  /* Animación de flotación para partículas */
  @keyframes float {
    0%, 100% { 
      transform: translate(0, 0) scale(1);
      opacity: 0.3;
    }
    25% { 
      transform: translate(30px, -30px) scale(1.2);
      opacity: 0.6;
    }
    50% { 
      transform: translate(60px, -15px) scale(0.8);
      opacity: 0.4;
    }
    75% { 
      transform: translate(30px, 15px) scale(1.1);
      opacity: 0.5;
    }
  }
  
  @keyframes float-alt {
    0%, 100% { 
      transform: translate(0, 0) scale(1);
      opacity: 0.4;
    }
    33% { 
      transform: translate(-40px, 25px) scale(1.3);
      opacity: 0.7;
    }
    66% { 
      transform: translate(-20px, -35px) scale(0.9);
      opacity: 0.5;
    }
  }
  
  /* Clases de utilidad para 3D */
  .perspective-1000 { perspective: 1000px; }
  .rotate-y-1 { transform: rotateY(1deg); }
  
  .animate-bounce-slow { animation: bounce 3s infinite; }
  
  /* Utilería Fade Up */
  @keyframes fade-in-up {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up {
      animation: fade-in-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      opacity: 0;
  }
</style>